import { describe, it, expect } from 'vitest';
import { SystemUtils } from '../../src/utils/system';

describe('SystemUtils', () => {
  it('should be defined', () => {
    expect(SystemUtils).toBeDefined();
  });
});